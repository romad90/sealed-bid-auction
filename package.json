{
  "name": "sealed-bid-auction",
  "version": "1.0.0",
  "description": "Algorithm for finding the winner and the winning price of a second-price, sealed-bid auction.",
  "main": "index.js",
  "scripts": {
    "test": "mocha -R spec",
    "lint": "standard .",
    "format": "prettier-standard 'lib/*.js' 'test/*.js' 'src/*.js' 'src/server/*.js' 'src/server/src/*.js'",
		"help": "./bin/computation --help",
    "api": "./bin/computation api-webservices | ./node_modules/.bin/pino-pretty -c -t",
		"local": "./bin/computation local-processing"
  },
  "author": "Rodier Madiande",
  "license": "ISC",
  "devDependencies": {
    "chai": "^4.2.0",
    "mocha": "^6.2.0",
    "standard": "^13.1.0"
  },
  "dependencies": {
    "commander": "^3.0.0",
    "pino-pretty": "^3.2.0",
    "prettier-standard": "^9.1.1"
  }
}
