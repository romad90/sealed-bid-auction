{
  "name": "server",
  "version": "1.0.0",
  "description": "Fastify server, calculating the bidder winning and the winning price of a second-price sealed-bid auction",
  "main": "index.js",
  "scripts": {
    "start": "node _index.js | ./node_modules/.bin/pino-pretty -c -t",
    "docs": "./node_modules/.bin/apidoc -i ./src -o docs/",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Rodier Madiande",
  "license": "ISC",
  "dependencies": {
    "fastify": "^2.7.1",
    "pino": "^5.13.2",
    "pino-pretty": "^3.2.0"
  },
  "devDependencies": {
    "apidoc": "^0.17.7"
  }
}
